pipeline {
    agent any

    stages {
         stage('Test') {
            steps {
            	script{
            	bat 'mvn clean test -Dtest=Runner -Dcucumber.options="--tags @all --plugin pretty" allure:serve'
            	}
                echo 'Testing..'
            }
        }
        
    }
}